.ipynb_checkpoints
*.pyc

# data that we cannot publish is in here 
unpublished

# figures or other data generated by notebooks
# NOTE: we need a more general solution here. Maybe name convention such as 'generated_files'
# and then simply exclude folders with this name?
applications/trypsin_benzamidine_hmm/figs

